# Лицензии и атрибуция данных

Код FlyMog под MIT (см. `LICENSE`). **Данные — нет.** Коннектом, предобученные
веса и наборы лиц живут в `data/`, в git не попадают и имеют собственные
лицензии, часть из которых может запрещать коммерческое использование.

> **Статус на конец M0: не выяснено окончательно.** Хосты, на которых лежат
> первоисточники (`zenodo.org`, `codex.flywire.ai`, `huggingface.co`,
> `drive.google.com`), заблокированы политикой egress того окружения, где
> писался код. Прочитать лицензию у первоисточника не удалось. Ниже —
> то, что удалось установить, и то, что осталось непроверенным.

## Сводка

| Актив | Лицензия | Статус проверки | Где лежит |
|---|---|---|---|
| Код FlyMog | MIT | установлено | этот репозиторий |
| `flyvis` (код) | MIT | **проверено**: классификатор `License :: OSI Approved :: MIT License` в `pyproject.toml` пакета, версия 1.2.0 на PyPI | зависимость |
| Данные коннектома внутри `flyvis` | неизвестно | **TODO(verify)** — код MIT не означает, что вшитые данные тоже MIT | зависимость |
| Предобученные веса `flyvis` | неизвестно | **TODO(verify)** | `data/flyvis_pretrained/` |
| Коннектом FlyWire (FAFB) | **спорно** | **TODO(verify)** — см. ниже | `data/connectome/` |
| Аннотации `flyconnectome/flywire_annotations` | неизвестно | **TODO(verify)** | `data/connectome/` |
| Референсный набор лиц | не выбран | решается в M2 | `data/reference_faces/` |

## Главный нерешённый вопрос: лицензия FlyWire

Поисковая выдача даёт **противоречивые** ответы: в одних местах данные FlyWire
описаны как CC BY 4.0, в других — как CC BY-NC 4.0 или CC BY-NC-SA 4.0.
Первоисточник (FAQ на `codex.flywire.ai` и запись на Zenodo) из окружения
разработки открыть не удалось: прокси вернул 403 по политике организации.

**Почему это не мелочь.** Если данные под NC, то бот, на котором когда-либо
появится платная функция, донат или реклама, нарушает лицензию. Решать это надо
**до** M1, а не после запуска.

Что нужно сделать (вручную, на машине с открытой сетью):

1. Открыть запись на Zenodo и FAQ Codex, найти точную формулировку лицензии.
2. Записать её сюда дословно, со ссылкой и датой обращения.
3. Если NC — либо отказаться от монетизации навсегда и написать это в README,
   либо заменить источник, либо запросить отдельное разрешение.

## Цитирование

`CITATION.cff` содержит список работ, но почти весь помечен `TODO(verify)`:
авторы, тома, страницы и DOI взяты из поисковой выдачи и метаданных пакетов, а
не из самих публикаций. **Не цитируй по нему, пока не сверишь с издателем.**
Выдумывать ссылки хуже, чем оставить пропуск, поэтому пропуски оставлены явно.

Кандидаты на цитирование (существование подтверждено, детали — нет):

- Lappalainen et al., «Connectome-constrained networks predict neural activity
  across the fly visual system», *Nature*, 2024 — модель `flyvis`.
- Работы по коннектому FlyWire. В выдаче фигурируют Dorkenwald et al. 2024 и
  Schlegel et al. 2024. **TODO(verify)**, какие именно и в какой формулировке
  просит цитировать сам консорциум.
- Shiu et al. 2024, вычислительная модель мозга дрозофилы — источник параметров
  LIF. Репозиторий `philshiu/Drosophila_brain_model` **существует** (проверено
  через `git ls-remote`), содержимое не читалось.

## Что проверено в этом окружении

| Хост | Результат |
|---|---|
| `pypi.org`, `files.pythonhosted.org` | доступен |
| `github.com` через git | доступен (`git ls-remote` работает) |
| `raw.githubusercontent.com` | доступен |
| `api.github.com` | только для этого репозитория |
| `zenodo.org` | **403, политика egress** |
| `codex.flywire.ai` | **403** |
| `huggingface.co` | **403** |
| `drive.google.com` | **403** |
| `download.pytorch.org` | **403** |

Команда `flymog fetch-data` перепроверяет это на любой машине и печатает точные
ссылки и пути для ручной загрузки.
